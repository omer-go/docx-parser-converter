// src/types/index.ts

// Re-export all types from this directory
export * from './common';
export * from './api';

// Example of a placeholder type if needed directly in index, though usually just re-exports
// export type PlaceholderType = string;
